# Ignore local Terraform directories
**/.terraform/*

# Ignore state files and backups (may contain sensitive data)
*.tfstate
*.tfstate.*

# Ignore crash log files
crash.log
crash.*.log

# Ignore variable files (may contain sensitive data)
*.tfvars
*.tfvars.json

# Ignore override files (used for local resource overrides)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore transient lock info files created during `terraform apply`
.terraform.tfstate.lock.info

# Ignore Terraform CLI configuration files
.terraformrc
terraform.rc

# Ignore Terraform plan output files (if applicable)
*.tfplan

.terraform.lock.hcl

tfplan

*password.txt
